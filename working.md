# Working
Items in process: one line per item, `- <ref> — since <date>, by <session agent or sub-agent>, at <plan step or gate>`.
Written by `/roadwork-make` when an item leaves "next" in `ordering.md` and becomes
"now"; drained by `/roadwork-verify-and-archive` at archiving.
